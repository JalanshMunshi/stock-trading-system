const router = require('express').Router();
module.exports = router;

// Get the portfolio of a user
router.get('/get-portfolio/:email', async (req, res, next) => {
    // Get all portfolios for a given email
});
