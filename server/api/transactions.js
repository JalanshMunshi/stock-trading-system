const router = require('express').Router();
module.exports = router;

// Get all transactions of a user
router.get('/get-transactions/:email', async (req, res, next) => {
    // Get all transactions for a given email
});
